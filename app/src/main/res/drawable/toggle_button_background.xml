<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State when button is checked -->
    <item android:state_checked="true">
        <shape android:shape="rectangle">
            <corners android:radius="8dp"/>
            <solid android:color="@color/colorPrimaryDark"/> <!-- Use a dark green for the checked state -->
            <stroke android:color="@color/colorPrimaryDark" android:width="1dp"/>
        </shape>
    </item>
    <!-- Default state when button is not checked -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="8dp"/>
            <solid android:color="@color/color3"/> <!-- Use a light green for the unchecked state background -->
            <stroke android:color="@color/colorPrimaryDark" android:width="1dp"/> <!-- Border in dark green -->
        </shape>
    </item>
</selector>
